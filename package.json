{
	"name": "42eat-web",
	"scripts": {
		"dev:front": "pnpm --filter ./front run dev",
		"dev:back": "pnpm --filter ./back run start:dev ",
		"dev": "concurrently -n back,front -c blue,green \"pnpm run dev:back\" \"wait-on tcp:localhost:3001 && pnpm run dev:front\"",
		"build": "concurrently -n back,front -c blue,green \"pnpm run build --filter ./front\" \"pnpm run build --filter ./back\""
	},
	"devDependencies": {
		"concurrently": "^9.2.1",
		"typescript": "^5.9.3",
		"wait-on": "^9.0.1"
	}
}