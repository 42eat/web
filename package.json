{
	"name": "42eat-web",
	"scripts": {
		"dev:front": "npm run dev --filter front",
		"dev:back": "npm run dev --filter back",
		"dev": "concurrently -n back,front -c blue,green \"npm run dev --filter server\" \"wait-on tcp:localhost:3001 && npm run dev --filter client\"",
		"build": "concurrently -n back,front -c blue,green \"npm run build --filter client\" \"npm run build --filter server\""
	},
	"devDependencies": {
		"concurrently": "^9.2.1",
		"wait-on": "^9.0.1"
	}
}